// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt
import GoogleRpc

/// Results of active probing from the last run of the test.
public struct ProbingDetails: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// The overall result of active probing.
  public var result: ProbingDetails.ProbingResult = ProbingDetails.ProbingResult()

  /// The time that reachability was assessed through active probing.
  public var verifyTime: GoogleCloudWkt.Timestamp? = nil

  /// Details about an internal failure or the cancellation of active probing.
  public var error: GoogleRpc.Status? = nil

  /// The reason probing was aborted.
  public var abortCause: ProbingDetails.ProbingAbortCause = ProbingDetails.ProbingAbortCause()

  /// Number of probes sent.
  public var sentProbeCount: Swift.Int32 = Swift.Int32()

  /// Number of probes that reached the destination.
  public var successfulProbeCount: Swift.Int32 = Swift.Int32()

  /// The source and destination endpoints derived from the test input and used
  /// for active probing.
  public var endpointInfo: EndpointInfo? = nil

  /// Latency as measured by active probing in one direction:
  /// from the source to the destination endpoint.
  public var probingLatency: LatencyDistribution? = nil

  /// The EdgeLocation from which a packet, destined to the internet, will egress
  /// the Google network.
  /// This will only be populated for a connectivity test which has an internet
  /// destination address.
  /// The absence of this field *must not* be used as an indication that the
  /// destination is part of the Google network.
  public var destinationEgressLocation: ProbingDetails.EdgeLocation? = nil

  /// Probing results for all edge devices.
  public var edgeResponses: [ProbingDetails.SingleEdgeResponse] = []

  /// Whether all relevant edge devices were probed.
  public var probedAllDevices: Swift.Bool = Swift.Bool()

  /// Initialize a new instance of `ProbingDetails`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = ProbingDetails().with { $0.result = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Representation of a network edge location as per
  /// https://cloud.google.com/vpc/docs/edge-locations.
  public struct EdgeLocation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Name of the metropolitan area.
    public var metropolitanArea: Swift.String = Swift.String()

    /// Initialize a new instance of `EdgeLocation`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = EdgeLocation().with { $0.metropolitanArea = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkmanagement.v1.ProbingDetails.EdgeLocation"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Probing results for a single edge device.
  public struct SingleEdgeResponse: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// The overall result of active probing for this egress device.
    public var result: ProbingDetails.ProbingResult = ProbingDetails.ProbingResult()

    /// Number of probes sent.
    public var sentProbeCount: Swift.Int32 = Swift.Int32()

    /// Number of probes that reached the destination.
    public var successfulProbeCount: Swift.Int32 = Swift.Int32()

    /// Latency as measured by active probing in one direction: from the source
    /// to the destination endpoint.
    public var probingLatency: LatencyDistribution? = nil

    /// The EdgeLocation from which a packet, destined to the internet, will
    /// egress the Google network.
    /// This will only be populated for a connectivity test which has an internet
    /// destination address.
    /// The absence of this field *must not* be used as an indication that the
    /// destination is part of the Google network.
    public var destinationEgressLocation: ProbingDetails.EdgeLocation? = nil

    /// Router name in the format '{router}.{metroshard}'. For example:
    /// pf01.aaa01, pr02.aaa01.
    public var destinationRouter: Swift.String = Swift.String()

    /// Initialize a new instance of `SingleEdgeResponse`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SingleEdgeResponse().with { $0.result = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.networkmanagement.v1.ProbingDetails.SingleEdgeResponse"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Overall probing result of the test.
  public enum ProbingResult: Codable, Equatable, Sendable {
    /// No result was specified.
    case unspecified
    /// At least 95% of packets reached the destination.
    case reachable
    /// No packets reached the destination.
    case unreachable
    /// Less than 95% of packets reached the destination.
    case reachabilityInconsistent
    /// Reachability could not be determined. Possible reasons are:
    /// * The user lacks permission to access some of the network resources
    ///   required to run the test.
    /// * No valid source endpoint could be derived from the request.
    /// * An internal error occurred.
    case undetermined
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .reachable: return 1
      case .unreachable: return 2
      case .reachabilityInconsistent: return 3
      case .undetermined: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "PROBING_RESULT_UNSPECIFIED"
      case .reachable: return "REACHABLE"
      case .unreachable: return "UNREACHABLE"
      case .reachabilityInconsistent: return "REACHABILITY_INCONSISTENT"
      case .undetermined: return "UNDETERMINED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ProbingResult/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "PROBING_RESULT_UNSPECIFIED": self = .unspecified
      case "REACHABLE": self = .reachable
      case "UNREACHABLE": self = .unreachable
      case "REACHABILITY_INCONSISTENT": self = .reachabilityInconsistent
      case "UNDETERMINED": self = .undetermined
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ProbingResult/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .reachable
      case 2: self = .unreachable
      case 3: self = .reachabilityInconsistent
      case 4: self = .undetermined
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .reachable: return try container.encode(1)
      case .unreachable: return try container.encode(2)
      case .reachabilityInconsistent: return try container.encode(3)
      case .undetermined: return try container.encode(4)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Abort cause types.
  public enum ProbingAbortCause: Codable, Equatable, Sendable {
    /// No reason was specified.
    case unspecified
    /// The user lacks permission to access some of the
    /// network resources required to run the test.
    case permissionDenied
    /// No valid source endpoint could be derived from the request.
    case noSourceLocation
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .permissionDenied: return 1
      case .noSourceLocation: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "PROBING_ABORT_CAUSE_UNSPECIFIED"
      case .permissionDenied: return "PERMISSION_DENIED"
      case .noSourceLocation: return "NO_SOURCE_LOCATION"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ProbingAbortCause/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "PROBING_ABORT_CAUSE_UNSPECIFIED": self = .unspecified
      case "PERMISSION_DENIED": self = .permissionDenied
      case "NO_SOURCE_LOCATION": self = .noSourceLocation
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ProbingAbortCause/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .permissionDenied
      case 2: self = .noSourceLocation
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .permissionDenied: return try container.encode(1)
      case .noSourceLocation: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networkmanagement.v1.ProbingDetails"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
