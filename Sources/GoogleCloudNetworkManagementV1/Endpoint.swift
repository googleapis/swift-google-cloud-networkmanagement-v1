// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// Source or destination of the Connectivity Test.
public struct Endpoint: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// The IP address of the endpoint, which can be an external or internal IP.
  public var ipAddress: Swift.String = Swift.String()

  /// The IP protocol port of the endpoint.
  /// Only applicable when protocol is TCP or UDP.
  public var port: Swift.Int32 = Swift.Int32()

  /// A Compute Engine instance URI.
  public var instance: Swift.String = Swift.String()

  /// A forwarding rule and its corresponding IP address represent the frontend
  /// configuration of a Google Cloud load balancer. Forwarding rules are also
  /// used for protocol forwarding, Private Service Connect and other network
  /// services to provide forwarding information in the control plane. Applicable
  /// only to destination endpoint. Format:
  /// `projects/{project}/global/forwardingRules/{id}` or
  /// `projects/{project}/regions/{region}/forwardingRules/{id}`
  public var forwardingRule: Swift.String = Swift.String()

  /// Output only. Specifies the type of the target of the forwarding rule.
  public var forwardingRuleTarget: Endpoint.ForwardingRuleTarget? = nil

  /// Output only. ID of the load balancer the forwarding rule points to. Empty
  /// for forwarding rules not related to load balancers.
  public var loadBalancerId: Swift.String? = nil

  /// Output only. Type of the load balancer the forwarding rule points to.
  public var loadBalancerType: LoadBalancerType? = nil

  /// A cluster URI for [Google Kubernetes Engine cluster control
  /// plane](https://cloud.google.com/kubernetes-engine/docs/concepts/cluster-architecture).
  public var gkeMasterCluster: Swift.String = Swift.String()

  /// DNS endpoint of [Google Kubernetes Engine cluster control
  /// plane](https://cloud.google.com/kubernetes-engine/docs/concepts/cluster-architecture).
  /// Requires gke_master_cluster to be set, can't be used simultaneoulsly with
  /// ip_address or network. Applicable only to destination endpoint.
  public var fqdn: Swift.String = Swift.String()

  /// A [Cloud SQL](https://cloud.google.com/sql) instance URI.
  public var cloudSqlInstance: Swift.String = Swift.String()

  /// A [Redis Instance](https://cloud.google.com/memorystore/docs/redis) URI.
  /// Applicable only to destination endpoint.
  public var redisInstance: Swift.String = Swift.String()

  /// A [Redis Cluster](https://cloud.google.com/memorystore/docs/cluster) URI.
  /// Applicable only to destination endpoint.
  public var redisCluster: Swift.String = Swift.String()

  /// A [GKE Pod](https://cloud.google.com/kubernetes-engine/docs/concepts/pod)
  /// URI.
  public var gkePod: Swift.String = Swift.String()

  /// A [DMS Private
  /// Connection](https://docs.cloud.google.com/database-migration/docs/reference/rest/v1/projects.locations.privateConnections)
  /// name format:
  /// projects/{project}/locations/{location}/privateConnections/{privateConnection}.
  public var dmsPrivateConnection: Swift.String = Swift.String()

  /// A [Cloud Function](https://cloud.google.com/functions). Applicable only to
  /// source endpoint.
  public var cloudFunction: Endpoint.CloudFunctionEndpoint? = nil

  /// An [App Engine](https://cloud.google.com/appengine) [service
  /// version](https://cloud.google.com/appengine/docs/admin-api/reference/rest/v1/apps.services.versions).
  /// Applicable only to source endpoint.
  public var appEngineVersion: Endpoint.AppEngineVersionEndpoint? = nil

  /// A [Cloud Run](https://cloud.google.com/run)
  /// [revision](https://cloud.google.com/run/docs/reference/rest/v1/namespaces.revisions/get)
  /// Applicable only to source endpoint.
  public var cloudRunRevision: Endpoint.CloudRunRevisionEndpoint? = nil

  /// A [Cloud Run](https://cloud.google.com/run)
  /// [job](https://docs.cloud.google.com/run/docs/reference/rest/v2/projects.locations.jobs#Job)
  /// URI.
  /// Applicable only to source endpoint.
  /// The format is: projects/{project}/locations/{location}/jobs/{job}
  public var cloudRunJob: Swift.String = Swift.String()

  /// A VPC network URI. For source endpoints, used according to the
  /// `network_type`. For destination endpoints, used only when the source is an
  /// external IP address endpoint, and the destination is an internal IP address
  /// endpoint.
  public var network: Swift.String = Swift.String()

  /// For source endpoints, type of the network where the endpoint is located.
  /// Not relevant for destination endpoints.
  public var networkType: Endpoint.NetworkType = Endpoint.NetworkType()

  /// For source endpoints, endpoint project ID. Used according to the
  /// `network_type`. Not relevant for destination endpoints.
  public var projectId: Swift.String = Swift.String()

  /// Initialize a new instance of `Endpoint`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Endpoint().with { $0.ipAddress = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Wrapper for Cloud Function attributes.
  public struct CloudFunctionEndpoint: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// A [Cloud Function](https://cloud.google.com/functions) name.
    public var uri: Swift.String = Swift.String()

    /// Initialize a new instance of `CloudFunctionEndpoint`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CloudFunctionEndpoint().with { $0.uri = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networkmanagement.v1.Endpoint.CloudFunctionEndpoint"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Wrapper for the App Engine service version attributes.
  public struct AppEngineVersionEndpoint: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// An [App Engine](https://cloud.google.com/appengine) [service
    /// version](https://cloud.google.com/appengine/docs/admin-api/reference/rest/v1/apps.services.versions)
    /// name.
    public var uri: Swift.String = Swift.String()

    /// Initialize a new instance of `AppEngineVersionEndpoint`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = AppEngineVersionEndpoint().with { $0.uri = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.networkmanagement.v1.Endpoint.AppEngineVersionEndpoint"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Wrapper for Cloud Run revision attributes.
  public struct CloudRunRevisionEndpoint: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// A [Cloud Run](https://cloud.google.com/run)
    /// [revision](https://cloud.google.com/run/docs/reference/rest/v1/namespaces.revisions/get)
    /// URI. The format is:
    /// projects/{project}/locations/{location}/revisions/{revision}
    public var uri: Swift.String = Swift.String()

    /// Output only. The URI of the Cloud Run service that the revision belongs
    /// to. The format is:
    /// projects/{project}/locations/{location}/services/{service}
    public var serviceUri: Swift.String = Swift.String()

    /// Initialize a new instance of `CloudRunRevisionEndpoint`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CloudRunRevisionEndpoint().with { $0.uri = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.networkmanagement.v1.Endpoint.CloudRunRevisionEndpoint"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// The type of the network of the IP address endpoint. Relevant for the source
  /// IP address endpoints.
  public enum NetworkType: Codable, Equatable, Sendable {
    /// Unspecified. The test will analyze all possible IP address locations.
    /// This might take longer and produce inaccurate or ambiguous results, so
    /// prefer specifying an explicit network type.
    ///
    /// The `project_id` field should be set to the project where the GCP
    /// endpoint is located, or where the non-GCP endpoint should be reachable
    /// from (via routes to non-GCP networks). The project might also be inferred
    /// from the Connectivity Test project or other projects referenced in the
    /// request.
    case unspecified
    /// A VPC network. Should be used for internal IP addresses in VPC networks.
    /// The `network` field should be set to the URI of this network. Only
    /// endpoints within this network will be considered.
    case gcpNetwork
    /// A non-GCP network (for example, an on-premises network or another cloud
    /// provider network). Should be used for internal IP addresses outside of
    /// Google Cloud. The `network` field should be set to the URI of the VPC
    /// network containing a corresponding Cloud VPN tunnel, Cloud Interconnect
    /// VLAN attachment, or a router appliance instance. Only endpoints reachable
    /// from the provided VPC network via the routes to non-GCP networks will be
    /// considered.
    case nonGcpNetwork
    /// Internet. Should be used for internet-routable external IP addresses or
    /// IP addresses for global Google APIs and services.
    case internet
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .gcpNetwork: return 1
      case .nonGcpNetwork: return 2
      case .internet: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "NETWORK_TYPE_UNSPECIFIED"
      case .gcpNetwork: return "GCP_NETWORK"
      case .nonGcpNetwork: return "NON_GCP_NETWORK"
      case .internet: return "INTERNET"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:NetworkType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "NETWORK_TYPE_UNSPECIFIED": self = .unspecified
      case "GCP_NETWORK": self = .gcpNetwork
      case "NON_GCP_NETWORK": self = .nonGcpNetwork
      case "INTERNET": self = .internet
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:NetworkType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .gcpNetwork
      case 2: self = .nonGcpNetwork
      case 3: self = .internet
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .gcpNetwork: return try container.encode(1)
      case .nonGcpNetwork: return try container.encode(2)
      case .internet: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Type of the target of a forwarding rule.
  public enum ForwardingRuleTarget: Codable, Equatable, Sendable {
    /// Forwarding rule target is unknown.
    case unspecified
    /// Compute Engine instance for protocol forwarding.
    case instance
    /// Load Balancer. The specific type can be found from [load_balancer_type]
    /// [google.cloud.networkmanagement.v1.Endpoint.load_balancer_type].
    case loadBalancer
    /// Classic Cloud VPN Gateway.
    case vpnGateway
    /// Forwarding Rule is a Private Service Connect endpoint.
    case psc
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .instance: return 1
      case .loadBalancer: return 2
      case .vpnGateway: return 3
      case .psc: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "FORWARDING_RULE_TARGET_UNSPECIFIED"
      case .instance: return "INSTANCE"
      case .loadBalancer: return "LOAD_BALANCER"
      case .vpnGateway: return "VPN_GATEWAY"
      case .psc: return "PSC"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ForwardingRuleTarget/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "FORWARDING_RULE_TARGET_UNSPECIFIED": self = .unspecified
      case "INSTANCE": self = .instance
      case "LOAD_BALANCER": self = .loadBalancer
      case "VPN_GATEWAY": self = .vpnGateway
      case "PSC": self = .psc
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ForwardingRuleTarget/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .instance
      case 2: self = .loadBalancer
      case 3: self = .vpnGateway
      case 4: self = .psc
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .instance: return try container.encode(1)
      case .loadBalancer: return try container.encode(2)
      case .vpnGateway: return try container.encode(3)
      case .psc: return try container.encode(4)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networkmanagement.v1.Endpoint"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
