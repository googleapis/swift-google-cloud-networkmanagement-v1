// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// A configuration to generate VPC Flow Logs.
public struct VpcFlowLogsConfig: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Identifier. Unique name of the configuration. The name can have one of the
  /// following forms:
  ///
  /// - For project-level configurations:
  /// `projects/{project_id}/locations/global/vpcFlowLogsConfigs/{vpc_flow_logs_config_id}`
  ///
  /// - For organization-level configurations:
  /// `organizations/{organization_id}/locations/global/vpcFlowLogsConfigs/{vpc_flow_logs_config_id}`
  public var name: Swift.String = Swift.String()

  /// Optional. The user-supplied description of the VPC Flow Logs configuration.
  /// Maximum of 512 characters.
  public var description: Swift.String? = nil

  /// Optional. The state of the VPC Flow Log configuration. Default value is
  /// ENABLED. When creating a new configuration, it must be enabled. Setting
  /// state=DISABLED will pause the log generation for this config.
  public var state: VpcFlowLogsConfig.State? = nil

  /// Optional. The aggregation interval for the logs. Default value is
  /// INTERVAL_5_SEC.
  public var aggregationInterval: VpcFlowLogsConfig.AggregationInterval? = nil

  /// Optional. The value of the field must be in (0, 1]. The sampling rate of
  /// VPC Flow Logs where 1.0 means all collected logs are reported. Setting the
  /// sampling rate to 0.0 is not allowed. If you want to disable VPC Flow Logs,
  /// use the state field instead. Default value is 1.0.
  public var flowSampling: Swift.Float? = nil

  /// Optional. Configures whether all, none or a subset of metadata fields
  /// should be added to the reported VPC flow logs. Default value is
  /// INCLUDE_ALL_METADATA.
  public var metadata: VpcFlowLogsConfig.Metadata? = nil

  /// Optional. Custom metadata fields to include in the reported VPC flow logs.
  /// Can only be specified if "metadata" was set to CUSTOM_METADATA.
  public var metadataFields: [Swift.String] = []

  /// Optional. Export filter used to define which VPC Flow Logs should be
  /// logged.
  public var filterExpr: Swift.String? = nil

  /// Optional. Determines whether to include cross project annotations in the
  /// logs. This field is available only for organization configurations. If not
  /// specified in org configs will be set to CROSS_PROJECT_METADATA_ENABLED.
  public var crossProjectMetadata: VpcFlowLogsConfig.CrossProjectMetadata? = nil

  /// Output only. Describes the state of the configured target resource for
  /// diagnostic purposes.
  public var targetResourceState: VpcFlowLogsConfig.TargetResourceState? = nil

  /// Optional. Resource labels to represent user-provided metadata.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Output only. The time the config was created.
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. The time the config was updated.
  public var updateTime: GoogleCloudWkt.Timestamp? = nil

  /// Reference to the resource of the config scope. That is, the scope from
  /// which traffic is logged. The target resource must belong to the same
  /// project as the configuration.
  /// This field is not supported for organization level configurations.
  public var targetResource: OneOf_TargetResource? = nil

  /// Initialize a new instance of `VpcFlowLogsConfig`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = VpcFlowLogsConfig().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case name = "name"
    case description = "description"
    case state = "state"
    case aggregationInterval = "aggregationInterval"
    case flowSampling = "flowSampling"
    case metadata = "metadata"
    case metadataFields = "metadataFields"
    case filterExpr = "filterExpr"
    case crossProjectMetadata = "crossProjectMetadata"
    case targetResourceState = "targetResourceState"
    case network = "network"
    case subnet = "subnet"
    case interconnectAttachment = "interconnectAttachment"
    case vpnTunnel = "vpnTunnel"
    case labels = "labels"
    case createTime = "createTime"
    case updateTime = "updateTime"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.description = try container.decodeIfPresent(Swift.String.self, forKey: .description)
    self.state = try container.decodeIfPresent(VpcFlowLogsConfig.State.self, forKey: .state)
    self.aggregationInterval = try container.decodeIfPresent(
      VpcFlowLogsConfig.AggregationInterval.self, forKey: .aggregationInterval)
    self.flowSampling = try container.decodeIfPresent(Swift.Float.self, forKey: .flowSampling)
    self.metadata = try container.decodeIfPresent(
      VpcFlowLogsConfig.Metadata.self, forKey: .metadata)
    self.metadataFields = try container.decode([Swift.String].self, forKey: .metadataFields)
    self.filterExpr = try container.decodeIfPresent(Swift.String.self, forKey: .filterExpr)
    self.crossProjectMetadata = try container.decodeIfPresent(
      VpcFlowLogsConfig.CrossProjectMetadata.self, forKey: .crossProjectMetadata)
    self.targetResourceState = try container.decodeIfPresent(
      VpcFlowLogsConfig.TargetResourceState.self, forKey: .targetResourceState)
    self.labels = try container.decode([Swift.String: Swift.String].self, forKey: .labels)
    self.createTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .createTime)
    self.updateTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .updateTime)

    var targetResource: OneOf_TargetResource? = nil
    let targetResourceCheckAndSet = {
      if targetResource != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'targetResource'"))
      }
      targetResource = $0
    }
    if let network = try container.decodeIfPresent(Swift.String.self, forKey: .network) {
      try targetResourceCheckAndSet(.network(network))
    }
    if let subnet = try container.decodeIfPresent(Swift.String.self, forKey: .subnet) {
      try targetResourceCheckAndSet(.subnet(subnet))
    }
    if let interconnectAttachment = try container.decodeIfPresent(
      Swift.String.self, forKey: .interconnectAttachment)
    {
      try targetResourceCheckAndSet(.interconnectAttachment(interconnectAttachment))
    }
    if let vpnTunnel = try container.decodeIfPresent(Swift.String.self, forKey: .vpnTunnel) {
      try targetResourceCheckAndSet(.vpnTunnel(vpnTunnel))
    }
    self.targetResource = targetResource
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.description, forKey: .description)
    try container.encode(self.state, forKey: .state)
    try container.encode(self.aggregationInterval, forKey: .aggregationInterval)
    try container.encode(self.flowSampling, forKey: .flowSampling)
    try container.encode(self.metadata, forKey: .metadata)
    try container.encode(self.metadataFields, forKey: .metadataFields)
    try container.encode(self.filterExpr, forKey: .filterExpr)
    try container.encode(self.crossProjectMetadata, forKey: .crossProjectMetadata)
    try container.encode(self.targetResourceState, forKey: .targetResourceState)
    try container.encode(self.labels, forKey: .labels)
    try container.encode(self.createTime, forKey: .createTime)
    try container.encode(self.updateTime, forKey: .updateTime)

    if let choice = self.targetResource {
      switch choice {
      case .network(let value):
        try container.encode(value, forKey: .network)
      case .subnet(let value):
        try container.encode(value, forKey: .subnet)
      case .interconnectAttachment(let value):
        try container.encode(value, forKey: .interconnectAttachment)
      case .vpnTunnel(let value):
        try container.encode(value, forKey: .vpnTunnel)
      }
    }
  }

  /// Determines whether this configuration will be generating logs.
  public enum State: Codable, Equatable, Sendable {
    /// If not specified, will default to ENABLED.
    case unspecified
    /// When ENABLED, this configuration will generate logs.
    case enabled
    /// When DISABLED, this configuration will not generate logs.
    case disabled
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .enabled: return 1
      case .disabled: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "STATE_UNSPECIFIED"
      case .enabled: return "ENABLED"
      case .disabled: return "DISABLED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:State/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "STATE_UNSPECIFIED": self = .unspecified
      case "ENABLED": self = .enabled
      case "DISABLED": self = .disabled
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:State/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .enabled
      case 2: self = .disabled
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .enabled: return try container.encode(1)
      case .disabled: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Toggles the aggregation interval for collecting flow logs by 5-tuple.
  public enum AggregationInterval: Codable, Equatable, Sendable {
    /// If not specified, will default to INTERVAL_5_SEC.
    case unspecified
    /// Aggregate logs in 5s intervals.
    case interval5Sec
    /// Aggregate logs in 30s intervals.
    case interval30Sec
    /// Aggregate logs in 1m intervals.
    case interval1Min
    /// Aggregate logs in 5m intervals.
    case interval5Min
    /// Aggregate logs in 10m intervals.
    case interval10Min
    /// Aggregate logs in 15m intervals.
    case interval15Min
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .interval5Sec: return 1
      case .interval30Sec: return 2
      case .interval1Min: return 3
      case .interval5Min: return 4
      case .interval10Min: return 5
      case .interval15Min: return 6
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "AGGREGATION_INTERVAL_UNSPECIFIED"
      case .interval5Sec: return "INTERVAL_5_SEC"
      case .interval30Sec: return "INTERVAL_30_SEC"
      case .interval1Min: return "INTERVAL_1_MIN"
      case .interval5Min: return "INTERVAL_5_MIN"
      case .interval10Min: return "INTERVAL_10_MIN"
      case .interval15Min: return "INTERVAL_15_MIN"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:AggregationInterval/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "AGGREGATION_INTERVAL_UNSPECIFIED": self = .unspecified
      case "INTERVAL_5_SEC": self = .interval5Sec
      case "INTERVAL_30_SEC": self = .interval30Sec
      case "INTERVAL_1_MIN": self = .interval1Min
      case "INTERVAL_5_MIN": self = .interval5Min
      case "INTERVAL_10_MIN": self = .interval10Min
      case "INTERVAL_15_MIN": self = .interval15Min
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:AggregationInterval/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .interval5Sec
      case 2: self = .interval30Sec
      case 3: self = .interval1Min
      case 4: self = .interval5Min
      case 5: self = .interval10Min
      case 6: self = .interval15Min
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .interval5Sec: return try container.encode(1)
      case .interval30Sec: return try container.encode(2)
      case .interval1Min: return try container.encode(3)
      case .interval5Min: return try container.encode(4)
      case .interval10Min: return try container.encode(5)
      case .interval15Min: return try container.encode(6)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Configures which log fields would be included.
  public enum Metadata: Codable, Equatable, Sendable {
    /// If not specified, will default to INCLUDE_ALL_METADATA.
    case unspecified
    /// Include all metadata fields.
    case includeAllMetadata
    /// Exclude all metadata fields.
    case excludeAllMetadata
    /// Include only custom fields (specified in metadata_fields).
    case customMetadata
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .includeAllMetadata: return 1
      case .excludeAllMetadata: return 2
      case .customMetadata: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "METADATA_UNSPECIFIED"
      case .includeAllMetadata: return "INCLUDE_ALL_METADATA"
      case .excludeAllMetadata: return "EXCLUDE_ALL_METADATA"
      case .customMetadata: return "CUSTOM_METADATA"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Metadata/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "METADATA_UNSPECIFIED": self = .unspecified
      case "INCLUDE_ALL_METADATA": self = .includeAllMetadata
      case "EXCLUDE_ALL_METADATA": self = .excludeAllMetadata
      case "CUSTOM_METADATA": self = .customMetadata
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Metadata/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .includeAllMetadata
      case 2: self = .excludeAllMetadata
      case 3: self = .customMetadata
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .includeAllMetadata: return try container.encode(1)
      case .excludeAllMetadata: return try container.encode(2)
      case .customMetadata: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Determines whether to include cross project annotations in the logs.
  /// Project configurations will always have CROSS_PROJECT_METADATA_DISABLED.
  public enum CrossProjectMetadata: Codable, Equatable, Sendable {
    /// If not specified, the default is CROSS_PROJECT_METADATA_ENABLED.
    case unspecified
    /// When CROSS_PROJECT_METADATA_ENABLED, metadata from other projects will be
    /// included in the logs.
    case enabled
    /// When CROSS_PROJECT_METADATA_DISABLED, metadata from other projects will
    /// not be included in the logs.
    case disabled
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .enabled: return 1
      case .disabled: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "CROSS_PROJECT_METADATA_UNSPECIFIED"
      case .enabled: return "CROSS_PROJECT_METADATA_ENABLED"
      case .disabled: return "CROSS_PROJECT_METADATA_DISABLED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:CrossProjectMetadata/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "CROSS_PROJECT_METADATA_UNSPECIFIED": self = .unspecified
      case "CROSS_PROJECT_METADATA_ENABLED": self = .enabled
      case "CROSS_PROJECT_METADATA_DISABLED": self = .disabled
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:CrossProjectMetadata/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .enabled
      case 2: self = .disabled
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .enabled: return try container.encode(1)
      case .disabled: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Output only. Indicates whether the target resource exists, for diagnostic
  /// purposes.
  public enum TargetResourceState: Codable, Equatable, Sendable {
    /// Unspecified target resource state.
    case unspecified
    /// Indicates that the target resource exists.
    case targetResourceExists
    /// Indicates that the target resource does not exist.
    case targetResourceDoesNotExist
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .targetResourceExists: return 1
      case .targetResourceDoesNotExist: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "TARGET_RESOURCE_STATE_UNSPECIFIED"
      case .targetResourceExists: return "TARGET_RESOURCE_EXISTS"
      case .targetResourceDoesNotExist: return "TARGET_RESOURCE_DOES_NOT_EXIST"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:TargetResourceState/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "TARGET_RESOURCE_STATE_UNSPECIFIED": self = .unspecified
      case "TARGET_RESOURCE_EXISTS": self = .targetResourceExists
      case "TARGET_RESOURCE_DOES_NOT_EXIST": self = .targetResourceDoesNotExist
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:TargetResourceState/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .targetResourceExists
      case 2: self = .targetResourceDoesNotExist
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .targetResourceExists: return try container.encode(1)
      case .targetResourceDoesNotExist: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Reference to the resource of the config scope. That is, the scope from
  /// which traffic is logged. The target resource must belong to the same
  /// project as the configuration.
  /// This field is not supported for organization level configurations.
  public enum OneOf_TargetResource: Codable, Equatable, Sendable {
    /// Traffic will be logged from VMs, VPN tunnels and Interconnect Attachments
    /// within the network.
    /// Format: projects/{project_id}/global/networks/{name}
    case network(Swift.String)
    /// Traffic will be logged from VMs within the subnetwork.
    /// Format: projects/{project_id}/regions/{region}/subnetworks/{name}
    case subnet(Swift.String)
    /// Traffic will be logged from the Interconnect Attachment.
    /// Format:
    /// projects/{project_id}/regions/{region}/interconnectAttachments/{name}
    case interconnectAttachment(Swift.String)
    /// Traffic will be logged from the VPN Tunnel.
    /// Format: projects/{project_id}/regions/{region}/vpnTunnels/{name}
    case vpnTunnel(Swift.String)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networkmanagement.v1.VpcFlowLogsConfig"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
