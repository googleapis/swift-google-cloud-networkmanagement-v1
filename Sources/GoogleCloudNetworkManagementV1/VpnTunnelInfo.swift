// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// For display only. Metadata associated with a Compute Engine VPN tunnel.
public struct VpnTunnelInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Name of a VPN tunnel.
  public var displayName: Swift.String = Swift.String()

  /// URI of a VPN tunnel.
  public var uri: Swift.String = Swift.String()

  /// URI of the VPN gateway at local end of the tunnel.
  public var sourceGateway: Swift.String = Swift.String()

  /// URI of a VPN gateway at remote end of the tunnel.
  public var remoteGateway: Swift.String = Swift.String()

  /// Remote VPN gateway's IP address.
  public var remoteGatewayIp: Swift.String = Swift.String()

  /// Local VPN gateway's IP address.
  public var sourceGatewayIp: Swift.String = Swift.String()

  /// URI of a Compute Engine network where the VPN tunnel is configured.
  public var networkUri: Swift.String = Swift.String()

  /// Name of a Google Cloud region where this VPN tunnel is configured.
  public var region: Swift.String = Swift.String()

  /// Type of the routing policy.
  public var routingType: VpnTunnelInfo.RoutingType = VpnTunnelInfo.RoutingType()

  /// Initialize a new instance of `VpnTunnelInfo`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = VpnTunnelInfo().with { $0.displayName = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Types of VPN routing policy. For details, refer to [Networks and Tunnel
  /// routing](https://cloud.google.com/network-connectivity/docs/vpn/concepts/choosing-networks-routing/).
  public enum RoutingType: Codable, Equatable, Sendable {
    /// Unspecified type. Default value.
    case unspecified
    /// Route based VPN.
    case routeBased
    /// Policy based routing.
    case policyBased
    /// Dynamic (BGP) routing.
    case `dynamic`
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .routeBased: return 1
      case .policyBased: return 2
      case .`dynamic`: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "ROUTING_TYPE_UNSPECIFIED"
      case .routeBased: return "ROUTE_BASED"
      case .policyBased: return "POLICY_BASED"
      case .`dynamic`: return "DYNAMIC"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:RoutingType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "ROUTING_TYPE_UNSPECIFIED": self = .unspecified
      case "ROUTE_BASED": self = .routeBased
      case "POLICY_BASED": self = .policyBased
      case "DYNAMIC": self = .`dynamic`
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:RoutingType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .routeBased
      case 2: self = .policyBased
      case 3: self = .`dynamic`
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .routeBased: return try container.encode(1)
      case .policyBased: return try container.encode(2)
      case .`dynamic`: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networkmanagement.v1.VpnTunnelInfo"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
